# Signature-Verification-System-using-SVM
A machine learning-based Signature Verification System that identifies whether a signature is genuine or forged. It uses OpenCV for image processing and an SVM model for classification. The system provides a simple Streamlit interface where users can upload a signature image and receive the verification result.
